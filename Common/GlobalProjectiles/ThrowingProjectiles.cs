namespace Synergia.Common.GlobalProjectiles {
    public class ThrowingProjectiles : GlobalProjectile {
        //List<int> ProjType = [ProjectileID.SnowBallFriendly, ProjectileID.Trimarang, ProjectileType<StalloyScrew>()];

        //public override bool InstancePerEntity { get { return true; } }
        //public override bool AppliesToEntity(Projectile entity, bool lateInstantiation) => ProjType.Contains(entity.type);
        //public override void OnHitNPC(Projectile projectile, NPC target, NPC.HitInfo hit, int damageDone) {
        //    if (projectile.owner >= 0 && projectile.owner < Main.maxPlayers) {
        //        Player player = Main.player[projectile.owner];
        //        if (player.GetModPlayer<ThrowingPlayer>().ActiveUI) {
        //            player.GetModPlayer<ThrowingPlayer>().AddCombo();
        //        }
        //    }
        //}
    }
}
